/**
 * Smart AI NIDS - Client Environment Configuration
 * Can be overridden dynamically by deployment environments or localStorage.
 */
window.ENV = window.ENV || {
  VITE_API_BASE_URL: "http://localhost:8000"
};
